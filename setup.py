from setuptools import setup, find_packages

setup(name="arraytotable",
      version="0.0.5",
      description="Convert an array to latex table",
      author="<NAME>",
      packages=find_packages(),
      install_requires=["numpy"]
      )